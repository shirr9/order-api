# env can be dev/local/prod
env: "dev"
# log_path: "" means logs just in os.Stdout
log_path: "C:\\Users\\User\\GolandProjects\\order-api\\logs\\logs.txt"

postgres:
  user: "new_user"
  password: ""
  #host: "order-api-db" for container-app
  host: "localhost"
  port: "5432"
  db_name: "db"
  sslmode: "disable"

redis:
  password: ""
  #  host: "order-api-cache" for container-app
  host: "localhost"
  port: "6379"
  # int max_size, default max_size = 20
  max_size: 10
  # ttl(time to live) - count of time.Second - int, default ttl = 300
  ttl: 600